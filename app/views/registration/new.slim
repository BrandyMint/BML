.row
  .col-sm-9.col-sm-offset-3
    .page-header
      h1 Регистрация
  .col-sm-5.col-sm-offset-3
    = simple_form_for registration_form, url: signup_url do |f|
      = f.error_notification
      /- if Rails.env.development?
        /p.alert.alert-warning
          /= f.object.errors.full_messages
      .form-inputs
        = f.input :name, requried: true, autofocus: true
        = f.input :email, requried: true
        = f.input :phone, requried: true
        = f.input :password, requried: true
      .form-actions
        = f.submit 'Зарегистрироваться', class: 'btn btn-primary'
