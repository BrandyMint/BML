.row
  .col-md-12
    .pull-right
      = link_to 'Добавить версию', new_account_landing_version_path, class: 'btn btn-success'

br

- if versions.any?

  table.table
   thead.thead-default
    tr
      th Версия
      th Срок действия
      th Посетителей
      th Посещений
      th Конверсия
      th

    tbody
      - versions.each do |v|
        tr
          td
            = link_to landing_editor_path(current_landing, v) do
              = v.to_s
          td 3 дня
          td 12 тыс
          td 10 тыс
          td 12%
          td.pull-right
            ul.list-inline
              li.list-inline-item
                = link_to landing_editor_path(current_landing, v) do
                  = fa_icon 'object-group'
              li.list-inline-item
                = link_to edit_account_landing_version_path(id: v.id, landing: current_landing) do
                  = fa_icon :pencil

- else
  .jumbotron
    h1 У вас пока нет ABC-тестов. 
    p Тесты проводятся через создание различных версий лендинга. Создайте новую версию, налейте трафика и увидите чем отличается конверсия одной версии от другой.
